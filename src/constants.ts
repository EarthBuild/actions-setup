export enum State {
  CachePrimaryKey = 'EARTHBUILD_CACHE_KEY',
  CacheMatchedKey = 'EARTHBUILD_CACHE_RESULT',
  BinaryPath = 'EARTHBUILD_BINARY_PATH',
  BuildkitCacheMatchedKey = 'BUILDKIT_CACHE_RESULT',
}

export enum Outputs {
  CacheHit = 'cache-hit',
}
